server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
#          uri: file:///Users/injae/git-local-repo
          uri: https://github.com/injae7034/spring-cloud-config
          default-label: master
